/* A C program to greet the user on the monitor/console. */

//Pre-processor directive(s)
#include <stdio.h>

//main function begins program execution
int main()
{
    //Create a character array to hold the user's name
    char userName[] = "Edwin";

    //Greet the user using a printf() functions
    printf("Hello %s from C Programming!",userName);
    return 0;
}
